: fib
    dup 0 = if
    else
        dup 1 = if
        else
            dup 1 - fib
            swap 2 - fib
            +
        then
    then
;


0 repeat
    1 +
    dup fib .
    dup 10 <
while
